pipeline {
    agent {
                docker { 
                        image 'licensefinder/license_finder:latest'                        
                }
            }
    stages {
        
        stage('test licensefinder') {
            
            steps {
                sh 'ls'
                sh 'echo $PWD'
                sh 'cd ..'
                sh 'ls'
                sh '/bin/bash -login'
                sh 'license_finder -p'
            }
            
        }
    }
}
