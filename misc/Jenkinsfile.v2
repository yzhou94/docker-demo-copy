node {
   def commit_id
   stage('Preparation') {
     checkout scm
     sh "git rev-parse --short HEAD > .git/commit-id"
     commit_id = readFile('.git/commit-id').trim()
   } 
   
   stage('test with licensefinder') {
     docker.image("licensefinder/license_finder:latest").inside('--volumes-from=$HOSTNAME -w ${WORKSPACE}'){
         sh 'ls'
         sh 'cd ..'
         sh 'ls'
         sh 'license_finder'
     }
   } 
   
                                      
}                   
